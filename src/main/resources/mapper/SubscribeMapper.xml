<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.SubscriptionMapper">
    <insert id="insertSubscription" parameterType="Subscribe">
        INSERT INTO Subscriptions (uid, subscription_type_id, storage_capacity, subscription_start_date, subscription_end_date) 
        VALUES (#{uid}, #{subscription_type_id}, #{storage_capacity}, #{subscription_start_date}, #{subscription_end_date})
      </insert>
</mapper>
